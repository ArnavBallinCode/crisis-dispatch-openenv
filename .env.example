# Local app server port (used by Docker CMD and local uvicorn if referenced)
PORT=7860

# Optional: only needed for inference.py --mode openai
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini

# Optional metadata helper
HF_SPACE_ID=blackmamba2408/Crisis-Dispatch-OpenEnv
